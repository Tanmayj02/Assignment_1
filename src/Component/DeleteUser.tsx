import { Button, Card, Container } from "react-bootstrap";
import userDetail from "./UserDetails"

  const DeleteUser = ({ name, email, phone, website }: userDetail) => {
    return (
        <Container>
            Delete
        </ Container>
    );
  };
  
  export default DeleteUser;
  